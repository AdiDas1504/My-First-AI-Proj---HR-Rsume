# TEST RUNBOOK — JobFit AI Resume Tailor

## 1. Purpose

This document explains how to run the project tests in practice.

The TEST_PLAN defines what should be tested.  
This TEST_RUNBOOK defines how to run the tests step by step.

---

## 2. Before Running Tests

Before running any test, confirm that the virtual environment is active.

Expected terminal prefix:

```powershell
(.venv)
```